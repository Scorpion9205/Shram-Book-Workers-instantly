export class SMSVerificationProvider {
    async sendOTP(phone, otp, name) {
        console.log(`SMS OTP for ${phone}: ${otp}`);
        // Future:
        // Twilio
        // Exotel
        // MSG91
    }
}
//# sourceMappingURL=sms-verification.provider.js.map